apiVersion: v1
kind: Service
metadata:
  name: ms-auth
spec:
  type: ClusterIP
  selector:
    app: ms-auth
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
    - name: tcp
      protocol: TCP
      port: 3100
      targetPort: 3100